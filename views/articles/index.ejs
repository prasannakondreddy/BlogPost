<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Satisfy&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link type="text/css" href="css/styles.css" rel="stylesheet">
    <title>Blog</title>
</head>
<body>
    <div class="container">
    <h1 class="heading">Blogpost</h1>
    <a href="/articles/new" class="btn new-btn mb-4">Add a new Article</a>
    <% articles.forEach(article=>{ %>
        <div class="card box box md-4">
        <div class="card-body">
            <h4 class="card-title title"><%= article.title %></h4>
            <div class="card-subtitle text-muted mb-2"><%= article.createdDate.toLocaleDateString() %></div>
            <div class="card-text mb-4 mt-4 des"><%= article.description %></div>
            <a href="articles/<%= article.id %>" class="btn btn-sm button">Read more</a>
            <a href="articles/edit/<%= article.id %>" class="btn btn-sm button">Edit</a>
            <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
                <button type="submit" class="btn btn-sm button">Delete</button>
        </div>
    </div>
    <% }) %>
    </div>
</body>
</html>